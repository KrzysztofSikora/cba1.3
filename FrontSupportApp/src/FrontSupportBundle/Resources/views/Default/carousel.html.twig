<div id="myCarousel" class="carousel slide" data-ride="carousel">

    <div class="carousel-inner" role="listbox">

        {% set it = 0 %}
        {% for image in images %}
            {#{% if image.status == 1 %}#}
                {% set it = it + 1 %}
                {% if it == 1 %}
                    {#{{ loop.index }}#}
                    {#{{ image.id }}#}
                    {#{{ it }}#}

        {#<td><a href="{{ path('cfile_show', { 'filename': carousel.cfilename }) }}"><img#}
                        {#src="{{ path('cfile_show', { 'filename': carousel.cfilename }) }}  " style="height: 250px"></a>#}
                    <div class="item active">
                        <img class="first-slide" style="margin: auto"
                             src="{{ path('cfile_show', { 'filename': image.cfilename }) }}" alt="First slide">
                        <div class="container">
                            <div class="carousel-caption">
                                <h1></h1>
                                <p></p>
                                <p></p>
                            </div>
                        </div>
                    </div>

                {% else %}
                    <div class="item">
                        <img class="second-slide" style="margin: auto"
                             src="{{ path('cfile_show', { 'filename': image.cfilename }) }}" alt="Second slide">
                        <div class="container">
                            <div class="carousel-caption">
                                <h1></h1>
                                <p></p>
                                <p></p>
                            </div>
                        </div>
                    </div>{% endif %}
            {#{% else %}{% endif %}#}
        {% endfor %}


    </div>
    <a class="left carousel-control" href="#myCarousel" role="button" data-slide="prev">
        <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
        <span class="sr-only">Previous</span>
    </a>
    <a class="right carousel-control" href="#myCarousel" role="button" data-slide="next">
        <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
        <span class="sr-only">Next</span>
    </a>
</div><!-- /.carousel -->
{% extends 'base.html.twig' %}

{% block body %}
    <h1>Dodaj album</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Dodaj" />
    {{ form_end(form) }}
    {{ err }}
    <ul>
        <li>
            <a href="{{ path('album_index') }}">Powrót do listy</a>
        </li>
    </ul>
{% endblock %}
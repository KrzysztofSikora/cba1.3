{% extends 'base.html.twig' %}

{% block body %}
    <h1>Dodawanie historii</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Dodaj" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('story_index') }}">Powrót do listy</a>
        </li>
    </ul>
{% endblock %}
